=== idx 715 ===
order is resting.

---

## A14 · ERROR CLASSIFICATION — TWO SPECIFIC CASES MATTER

```
ConnectionLostError
  "Operations in flight when the connection was lost have an indeterminate outcome."
  → affected orders become INDETERMINATE and are resolved by RECONCILIATION.
  → NEVER blind-retry. A blind retry can DOUBLE-FILL.

"Global Rate Limit Exceeded"
  → This is a TRANSIENT LATENCY REJECT, not a rate limit.
  → Retry WITHOUT backoff. Applying backoff here loses the window entirely.
```

---

## A15 · API SURFACE — EXACTLY THESE ROUTES

```
CONTROL     /start  /pause  /resume  /stop
STATE       /status  /settings  /history
LIVE        /ws                                    websocket, much smoother than polling
STRATEGY    /strategies  /strategies/{id}  /strategies/{id}/config
RESEARCH    /backtest  /orderbook

DROPPED     /health   — PM2 handles restarts; nothing external monitors it
```

`/start` boots the entire selected mode with one click, V1 or V2.

---

## A16 · DASHBOARD — SIXTEEN PRIMARY ELEMENTS

Visible without scrolling or switching tabs:

```
Polymarket timer · Current market · PTB · Current TWAP · Locked Trigger · Current Window ·
Direction · Trigger Status · Order Status · Position · Balance · Running P/L ·
Trade History · Logs · Strategy selector · Settings
```

Secondary (below the fold or a second tab): Order Book Monitor · Runtime Statistics (uptime,
markets processed, CPU, memory) · Market Timeline · Backtesting page.

**One chart only:** current market, signal TWAP line, PTB line, locked trigger line, window
markers, fire point. No indicators, no overlays, no second chart.

**Countdown:** real `MM:SS` matching Polymarket, server-synced, smooth, floored (04:59 for the
whole 299th second), **never negative**.

**Derived-math displays are removed** from live panels — no "required deviation", no
multiplier, no `n/k`. One exception: the compact `(~$60)` on the Active Window panel, and the
implied-BTC-move figure on Settings buffer rows while tuning.

**Stale data is never rendered as live.** On socket drop, values grey out.

---

## A17 · STRATEGY POLICY

**ONE strategy is implemented: `arc_twap_locked_buffer`** — the operator's own TWAP + Locked
Buffer strategy. Default, pinned, enabled, primary on the main dashboard and main timer, not
disableable.

The plugin **protocol** ships so nothing needs retrofitting later:

```python
class Strategy(Protocol):
    id: StrategyId
    display_name: str
    def evaluate(self, ctx: StrategyContext) -> StrategyDecision: ...
    def describe(self) -> StrategyDescription: ...
```

**Hard boundaries:** a strategy is a **pure function** — no I/O, no clock, no socket, no DB. It
cannot place, cancel, reprice, or size orders, and cannot bypass Risk. One strategy is active
per market. `arc/execution/` must grep clean of `strategy`, `twap`, `ptb`, and `buffer`.

**Every parameter of every strategy is configurable. Never hardcode a strategy value.**
Presets `Aggressive 3s` and `Conservative 15s` are **config presets of the default**, not
separate strategies.

**Additional strategies are DEFERRED, not cancelled.** Gate to build them: **100+ real markets
of V1 data on the default strategy.** Do not stub them, do not register them, do not leave dead
code. Rationale: buffers were calibrated against snapshot settlement and now face a different
outcome function — you cannot tune five things against zero data.

**No recommendation engine, no ranking, no scoring, no ML.** The operator chooses the
strategy. Raw per-market recording survives — that is the dataset, not a scoring system.

---

## A18 · BACKTESTING — A VIEWER, NOT A BACKTESTER

```
BUILD      1. historical BTC candles  (public 5m klines, fetched once, cached in SQLite)
           2. signal generation       (replay ARC's own TWAP + buffer + trigger math)
           3. trigger visualization   (candles · signal TWAP · PTB · trigger · fire points)

DO NOT BUILD   win rate · average return · max drawdown · Sharpe · equity curve ·
               optimizer · parameter sweep · walk-forward · ANY performance number
```

Non-dismissible header: *"Signal visualization only. Not performance. Polymarket settles on a
30-second TWAP; spot candles cannot reproduce settlement outcomes."*

**This is historical price data for research. It is NOT the return of Binance direction logic**
— that stays permanently deleted from the trading path. **Real validation is Paper Mode.**

---

## A19 · OUTPUT DISCIPLINE — EVERY PHASE

```
1  Read the phase prompt fully before writing any code.
2  If ANYTHING is unclear — even something small — STOP AND ASK. Do not assume.
   The operator has said: "I would rather answer ten questions now than rewrite code later."
3  Write complete, working code. Every function real. No placeholders.
4  Write the tests named in the phase's Tests Required section. Real assertions.
5  Each non-obvious decision gets a comment saying WHAT FAILURE IT PREVENTS — not what
   the code does.
6  Report at the end: files created · decisions made and why · anything you could not
   verify · the exact command to validate.
7  DO NOT claim a phase is validated if you have not executed the tests. Say plainly
   that they are written but unrun.
8  Do not proceed to the next phase until this one is green.
```

**Comment style — required.** Not `# floor the price` but:

```python
# Floor BEFORE validating. Validating then rounding lets the rounding push the price
# back over ENTRY_PRICE_MAX with every check having passe
=== idx 6101 ===
unless Prompt 6 explicitly requires it."* It must be a NEW entry point; `arc/runtime/observe.py` states *"there is no execution adapter in the import graph at all"* and must stay that way; `arc/cli.py::run()` currently prints a refusal and returns 1. Confirmed this segment: nothing outside `v2_live.py` itself imports `LiveExecutor`, so the runtime entry point remains the only missing integration point.
- **Unscheduled:** the Limit Order Engine dashboard panel (no FastAPI app, no `arc/api/` yet; A15's route list and A16's element list govern).

8. Current Work:

Executing the queued Pre-Prompt 6 corrections. Six corrections applied; `python -m ruff check .` → `All checks passed!` and `python -m mypy arc/` → `Success: no issues found in 63 source files`; `tests/test_providers.py` (22) and `tests/test_v2_live.py` (39) both pass in isolation.

The full suite then returned **exit=1** with two failures, both in `tests/test_window_stress.py::TestNoMemoryGrowth`:
```
FAILED tests/test_window_stress.py::TestNoMemoryGrowth::test_closed_market_instances_are_collected
FAILED tests/test_window_stress.py::TestNoMemoryGrowth::test_no_background_task_or_timer_is_left_behind
E       AssertionError: 5 asyncio Tasks exist after the run
E       assert not [<Task finished name='Task-19744' coro=<LiveExecutor.place() done, defined at C:\Users\AMITH\OneDrive\Desktop\lufy\arc\execution\v2_live.py:142> exception=ArcError('garbage')>]
```
I had just read `tests/test_window_stress.py:320-375` and established that both tests sweep `gc.get_objects()` process-wide, so they are picking up finished-with-exception `asyncio.Task` objects left behind by the new `tests/test_v2_live.py` — `'garbage'` is the message from my own `polymarket.UnexpectedResponseError("garbage")` parametrization in `TestFailureClassification`. A Task that ended in an exception retains the exception and traceback, which keeps it reachable until collected. This is cross-test contamination introduced by the new file, not a product leak, and the fix belongs in the new tests (or in a `gc.collect()`-and-drop discipline), not in the stress test's assertions.

9. Optional Next Step:

Fix the test-isolation contamination so `tests/test_window_stress.py::TestNoMemoryGrowth` passes in a full-suite run — the cleanest route is to stop `tests/test_v2_live.py` leaving exception-carrying Tasks reachable (e.g. an autouse fixture in that file that runs `gc.collect()` after each test, and/or consuming the raised exception so its traceback does not pin the Task). Then re-run `python -m ruff check .`, `python -m mypy arc/`, and `timeout 1800 python -m pytest -q -p no:randomly` to a clean exit, commit, and deliver the nine-item completion report.

This is directly in line with the governing instruction: *"If implementation differs from the specification, update it before Prompt 6"* with *"update affected tests"* and the requirement to run the *"full test suite, Ruff, MyPy; fix regressions"* — and with A19's *"DO NOT claim a phase is validated if you have not executed the tests."* The report cannot be delivered claiming synchronization while the suite exits 1.

Note on authorization for what remains: the user wrote *"If you discover any ambiguity that would change trading strategy, execution logic, order behaviour, or risk behaviour, stop and ask me one concise question before proceeding."* The SDK swap changed no strategy, sizing, direction, or risk semantics — only the venue adapter A5 names by distribution — and the same message says *"For every other engineering decision, you are authorized to decide and implement it without asking."* One item is worth flagging in the report rather than silently deciding: the official SDK's `post_only_would_cross` reject code has no `py_clob_client` equivalent and is currently classified as a definite `ArcError` (not a retry), on the reasoning that the limit price is frozen upstream so a retry could not succeed.

If you need specific details from before compaction (like exact code snippets, error messages, or content you generated), read the full transcript at: C:\Users\AMITH\.claude\projects\C--Users-AMITH-OneDrive-Desktop-lufy\b9eab4a2-6fcc-474e-b2cf-c1db3f02912a.jsonl
Continue the conversation from where it left off without asking the user any further questions. Resume directly — do not acknowledge the summary, do not recap what was happening, do not preface with "I'll continue" or similar. Pick up the last task as if the break never happened.
